const { isPalindrome } = require('./palindromo');

// test('Multiply by zero', () => {
//     expect(multiply(5,0)).toBe(0);
// });


test('Discover Palindromos in simple strings', () => {
    expect(isPalindrome('ana')).toBeTruthy(true);
});


